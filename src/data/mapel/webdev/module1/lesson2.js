export const lesson2 = {
	id: "2",
	name: "HyperText Markup Language",
	content:
		"An HTML document typically begins with a `<!DOCTYPE html>` declaration, followed by an <code>&lt;html&gt;</code> root element. Inside `<html>`, you'll find `<head>` (for metadata) and `<body>` (for visible content). Understanding this hierarchy is crucial for building well-formed pages. <pre><code>Ini coba</code></pre> Halo<code>Test</code>",
};
